<strong>
  <% likes.each.with_index do |like, index| %>
      <% if likes.size == 1 %>
          <%= like.user.name %> </strong> 赞了这条动态
      <% elsif like == likes.last %>
          </strong>和<strong>
  <%= + like.user.name %></strong> 赞了这条动态
      <% elsif index > 1 %>
          </strong><%= "and " + (likes.size-index).to_s + " others " %> 赞了这条动态
          <% break %>
      <% elsif index==likes.size-2 || index==1%>
          <%= like.user.name %>
      <% else %>
          <%= like.user.name + ", " %>
      <% end %>
  <% end %>
  </strong>